# Build output
/bin/
*.test
*.out

# Building inside an example module drops the binary next to its source, named
# after the directory. Build with -o /dev/null instead; this is the backstop.
/examples/*/go-client

# Agent working files — never committed
.agent/
.superpowers/
CLAUDE.md
AGENTS.md

# macOS
.DS_Store

# Profiles from `make bench-profile`.
*.prof
